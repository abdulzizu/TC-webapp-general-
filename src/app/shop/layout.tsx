import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Shop — Thrift Collision",
  description: "Browse all curated thrifted streetwear. Filter by category, size, colour, price. One-of-one pieces, dropped weekly.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
