import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Returns Policy — Thrift Collision",
  description: "How to return items. Same-day initiation, original condition required. Contact us via WhatsApp, email, or Instagram.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
