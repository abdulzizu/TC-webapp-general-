import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Terms of Service — Thrift Collision",
  description: "Terms and conditions for using Thrift Collision. Products, payments, delivery, returns, and governing law.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
